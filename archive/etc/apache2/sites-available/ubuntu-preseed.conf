<IfModule alias_module>
        alias /setup /srv/www/setup

        <Directory /srv/www/setup>
                Options Indexes FollowSymLinks
                AllowOverride All
                Require all granted
        </Directory>

        ErrorLog ${APACHE_LOG_DIR}/error.log
	CustomLog ${APACHE_LOG_DIR}/access.log combined
</IfModule>

